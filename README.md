
Expense Manager Microservices

Run:

1. Install Docker
2. Extract project
3. Open terminal inside folder

Run:
docker-compose up --build

API Examples:

GET http://localhost/users
POST http://localhost/users

GET http://localhost/expenses
GET http://localhost/categories
GET http://localhost/budgets
GET http://localhost/reports

ActiveMQ dashboard:
http://localhost:8161
user: admin
pass: admin
